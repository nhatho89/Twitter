<button
  class="follow-toggle"
  data-user-id="<%= user.id %>"
  data-initial-follow-statement="<%= current_user.follows?(user) ? 'followed' : 'unfollowed' %>"
  type="button"
  name="button">
</button>
<br>
